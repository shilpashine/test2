import { ApphightDirective } from './apphight.directive';

describe('ApphightDirective', () => {
  it('should create an instance', () => {
    const directive = new ApphightDirective();
    expect(directive).toBeTruthy();
  });
});
